#include <stdlib.h>
#include <stdio.h>

struct Node {
    char *data;
    struct Node *next;
};

struct StackLinked {
    char *first;
};

int stack_size(char *stack) {
    int count = 0;
    return count;
}
